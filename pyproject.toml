[project]
name = "cadence"
version = "0.1.0"
description = "Cadence — daily speaking practice. FastAPI analysis backend."
requires-python = ">=3.12"
dependencies = [
    "deepgram-sdk>=7.6.0",
    "fastapi>=0.140.0",
    "google-genai>=1.0.0",
    "python-dotenv>=1.0.0",
    "python-multipart>=0.0.32",
    "uvicorn>=0.51.0",
    "wordfreq>=3.1.1",
]
